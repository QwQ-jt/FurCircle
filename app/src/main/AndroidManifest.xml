<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========== 网络权限 ========== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- ========== 通知权限 (Android 13+) ========== -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ========== 开机自启权限 ========== -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ========== 电池优化白名单权限 ========== -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- ========== 安装未知应用权限（Android 8.0+） ========== -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- ========== 悬浮窗权限 ========== -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- ========== 陀螺仪传感器 ========== -->
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />

    <!-- ========== 存储权限（分版本兼容） ========== -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FurCircle"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- 主入口 Activity（透明中转） -->
        <activity
            android:name=".mian.FurMian"
            android:exported="true"
            android:theme="@style/Theme.FurCircle.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 地基 Activity -->
        <activity
            android:name=".activity.FdActivity"
            android:exported="false"
            android:theme="@style/Theme.FurCircle" />

        <!-- 启动页 Activity -->
        <activity
            android:name=".activity.StartupActivity"
            android:exported="false"
            android:theme="@style/Theme.FurCircle.FullScreen" />

        <!-- 保活服务 -->
        <service
            android:name="com.xrs.service.KeepAliveService"
            android:exported="false" />

        <!-- OTA 升级服务 -->
        <service
            android:name="com.xrs.service.OtaService"
            android:exported="false" />

    </application>

</manifest>
