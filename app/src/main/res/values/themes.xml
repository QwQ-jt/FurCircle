<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.FurCircle" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_500</item>
    </style>

    <!-- 透明主题（用于 FurMian 中转页） -->
    <style name="Theme.FurCircle.Transparent" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <!-- 全屏主题（用于 StartupActivity 启动页） -->
    <style name="Theme.FurCircle.FullScreen" parent="Theme.FurCircle">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
